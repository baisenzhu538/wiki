# OCR+VLM 识别：一堂DOC-20260816015737.png

> 1720×1538 | 3 段 | MiniMax-M3 | 课程上下文注入

## 段1 ✅

```markdown
00-Cubox_Inbox/ (原始入口)
│
├── Step 1: 提取文章字段
│   标题、摘要、正文前 500 字、来源类型、收藏时间
│
├── Step 2: 关键词预匹配 (记录但不直接使用)
│   作为模型参考输入，不作为最终结果
│
├── Step 3: LongCat 模型分类
│   输入：标题 + 摘要 + 正文 + 关键词建议
│   [后续内容无法辨认]
```

---

## 段2 ✅

```markdown
输入：高熵、消解·压实·关键问题
输出：目录 ID、标签、质量等级、SS 课题、置信度
内部先执行 T1（CEO 核心课题）扫描

- Step 4：结果校验与修正
  校验目录 ID 有效性
  校验标签在标签库内
  T1 命中则强制 quality：S

- Step 5：写入 frontmatter
  在文件头部插入 YAML 元数据

- Step 6：移动文件到目标目录
```

---

## 段3 ✅

**Step 6: 移动文件到目标目录**

&emsp;&emsp;创建目录（如不存在）

&emsp;&emsp;移动文件


**Step 7: 生成分类日志**

&emsp;&emsp;记录每篇文章的分类结果，供后续审计

---

