# 洪七公每日复盘 | 2026-06-27

## 今天产生了什么新资产？

完成 `00_inbox/一堂五步法之增长` 全目录 59 张高密度知识图片的多模态预处理：

| 资产 | 数量 | 路径 |
|---|---|---|
| PaddleOCR 文本 | 59 | `00_inbox/一堂五步法之增长/{stem}_paddle_ocr.txt` |
| MiniMax-M3 VLM 结构化描述 | 59 | `00_inbox/一堂五步法之增长/{stem}_vlm_desc.md` |
| VLM 描述清单 | 1 | `00_inbox/一堂五步法之增长/README-VLM描述汇总.md` |
| 素材处理总汇总 | 1 | `00_inbox/一堂五步法之增长/README-素材处理总汇总.md` |

质量指标：
- VLM 成功率：59/59（100%）
- 平均置信度：0.94
- 最低置信度：0.85
- 双层 JSON 解析错误修复：19/19

## 今天发现了什么新问题/阻塞？

1. **Git Bash 下 `python3` 命令不存在**：系统 Python 路径是 `C:/Program Files/Python312/python`，脚本调用需用 `python` 而非 `python3`。
2. **`.env` 文件未 export 时 Python 读不到**：`source ~/.hermes/.env` 不加 `set -a` 只会产生 shell 局部变量。已在临时脚本中加入从 `~/.hermes/.env` 直接读取的 fallback。
3. **MiniMax-M3 双层 JSON 问题在本批次大面积出现**：59 张中有 19 张触发 `_parse_error: true, confidence: 0.3`，正确 JSON 被嵌套在 `description` 字段的 markdown fence 内。已用 `json_repair` 批量修复。
4. **背景任务在长时间 VLM 批次中不够稳定**：首次后台任务因 `python3` 缺失失败，第二次因 env 导出方式失败，后续需改用 `set -a` + `python` + `disable_timeout=true`。

## 下次启动最需要记住什么？

1. **调用 MiniMax 脚本前**：确认用 `set -a && source ~/.hermes/.env && set +a && python <script>`，或直接使用已加 `.env` fallback 的脚本。
2. **VLM 批量任务完成后必须跑双层 JSON 修复**：即使汇总显示 0 失败，也要扫描 `_parse_error` 标记。建议把 `json_repair` 修复逻辑固化到 `describe-images-minimax.py` 主脚本中。
3. **大图/高密度图处理慢**：`truman-渠道工业化生产-全策略集-极其重要.png`（3711x5348）等超大图会显著拖慢批次，未来可预先 resize 或单独处理。
4. **产出交接**：本目录视觉原料（OCR 文本 + VLM 结构）已 ready，下一步应交老顽童进入 KDO 卡片生产管线，优先编译 `渠道增长飞轮`、`渠道工业化生产`、`渠道探索方法论` 三个主题域。

---

# 追加：老顽童 Hermes CLI Kimi API Key 修复 | 2026-06-27 晚间

## 问题
老顽童 Hermes CLI 报 401：`Provider: kimi-coding, Model: kimi-for-coding, Endpoint: https://api.kimi.com/coding`。旧 key 失效。

## 修复动作
1. 更新 WSL `~/.hermes/.env` 中 `KIMI_API_KEY` 为新 key（`sk-kimi-lcsFp...Ts2D`）。
2. 同步更新 `~/.hermes/profiles/laowantong/.env` 中的 `KIMI_API_KEY`。
3. 清除 WSL `~/.hermes/auth.json` 中 `kimi-coding` credential 缓存。
4. 用 curl/Python 直连 `https://api.kimi.com/coding/v1/chat/completions` 验证新 key → **HTTP 200**。
5. 尝试启动 `hermes-gateway-laowantong.service` → 因 Feishu app_id 与现有 gateway（wangyuyan, PID 366）冲突而退出，这是预期行为。已停止并禁用 laowantong gateway service，避免无意义循环重启。

## 结论
- Kimi API key 已生效，老顽童 CLI 可用。
- 使用方式：在 WSL 中运行 `hermes --profile laowantong`（CLI 模式）。
- 不能同时启动多个使用同一 Feishu app_id 的 Hermes gateway，因此 laowantong 只能用 CLI 模式，不能开 gateway。

## 下次记住
- Hermes 的 `.env` 优先级：WSL `~/.hermes/.env` 是全局真相源；profile 目录下的 `.env` 也可能被读取，关键 key 要两处同步。
- 换 key 后必须清 `auth.json` 的 credential_pool 缓存，否则 Hermes 可能继续用旧 key。
- 验证 key 最直接的方法：用 `curl` 或 Python 直连 API endpoint，不要只看 `hermes status`。
