# 建议书：C 域跨域骨干直连——Obsidian 图谱孤岛问题

**致**：王语嫣
**自**：欧阳锋
**日期**：2026-07-12
**事由**：用户观察 Obsidian 图谱视图中业务公式域（C 域）呈孤岛状，请我诊断。数据坐实非错觉，现报诊断结论与编排建议，请你拆成任务派发。

---

## 一、诊断数据（python 全库实测，非估算）

| 维度 | 数据 |
|:--|:--|
| 域内边 | 763 条（去重，56 卡+digest）——内部极密，#158 终审已验零孤儿 |
| 出向跨域 | 仅 59 个目标，且高度集中（关键假设系 5 卡引用、ABCD/三环过滤器/双三角武器库各 2-3） |
| **入向跨域** | **112 条边仅覆盖 21/56 张卡——35 张 C 域卡从外部完全不可达** |
| 死链 | 5 条 `ocr-一堂-*` 链接指向不存在的卡（全库历史病，C 域卡里恰有 5 处） |

## 二、诊断结论：缺的不是桥，是骨干直连

3 张黄药师桥接卡（双三角/Y模型/基本功）+ ABCD 模型卡确实连着 C 域，但**桥卡本身是图的边缘节点**——draft 状态、单点中转。主卡与主卡之间零直连。已 grep 坐实的外部 hub 缺口（这些卡 currently 零条 C 域出链）：

1. `framework-一堂-苦练基本功-总纲` → C 域：**0 条**（两域号称"同一套 OS 两个对象"，总纲互不相识）
2. `framework-一堂-基本功-九层金字塔` → C 域：0 条
3. `framework-一堂-基本功-四字诀拆建推练` → C 域：0 条
4. `concept-yihang-dual-triangle-core` → C 域：0 条
5. `yt-decision-y-model` → C 域：0 条
6. `system-yitang-Y-model-os` → C 域：0 条

反向（C→这些 hub）部分存在（如 yt-decision-y-model 被 1 张 C 卡引用），**执行前须逐条 grep 双向确认，只补缺的方向**，避免重复建边。

## 三、编排建议（三个任务，可并行）

### 任务 A：C 域骨干直连（P1，建议派老顽童）

- **内容**：上表 6 个外部 hub + C 域总纲之间补双向 related，候选边 ~12-18 条；另并入已裁定的 T4（`framework-yitang-y-model-cross-domain-fusion` L74 业务公式行补 C 总纲链）
- **候选边清单**（执行前 grep 确认缺口方向）：
  - 苦练基本功总纲 ↔ 业务公式总纲（互译关系见桥接卡 §一）
  - 九层金字塔 → 参数冰山/六层逻辑（同构映射）
  - 四字诀拆建推练 → 业务公式总纲（拆建推练 ↔ 梳理-建公式-探参数-探逻辑-假设管理）
  - dual-triangle-core ↔ 业务公式总纲（六顶点定位）
  - yt-decision-y-model ↔ 业务公式总纲（关键假设层操作化）
  - Y-model-os → 业务公式总纲（Agent OS 底座对接）
  - ABCD 模型卡 ↔ C 总纲（ABCD→C 已有 4 条边，查 C→ABCD 是否缺）
- **验收口径**：①每边双向，grep 两卡 related 互见；②触碰文件逐件 pre-submit PASS；③lint 增量（T5 回卷后）这些边零新增债；④申报集=实动集
- **量级**：小任务，半天内。不改正文，只动 related + 必要的 Synthesis 一行

### 任务 B：同类型债修复——跨域子集优先（P1，黄药师 manifest + 执行）

- 黄药师基线修复方案中「同类型 2769 条真债」按我已裁定的方向推进，**但排序调整：跨域 framework↔framework / concept↔concept 子集先做**——量小、对图谱结构改善最大，正是本孤岛问题的另一半解药
- 豁免规则铁律不变：按方向写不按类型对写，引用型正向边（case→concept 等）才豁免，反向仍报债；例外清单逐条带 reason 我过目
- **验收口径**：manifest 逐批，我抽验 ≥10%；每批 pre-submit + 增量 lint 双过

### 任务 C：`ocr-*` 死链查证（P2，黄药师）

- C 域 5 处 `ocr-一堂-*` 链接指向不存在的卡，全库同类病散布在大量 case 卡（历史遗留，非本轮引入）
- 查证方向：目标是改名了（改指现名）还是该指 raw 笔记（摘链改纯文本标注）
- **验收口径**：逐条给处置清单（改/摘/例外），我过目后批量执行

## 四、排序与依赖

- 任务 A、B、C 互不阻塞，可并行
- 任务 B 依赖 T5（基线纠正回卷）——已第三次重申，是黄药师侧所有 lint 相关工作的硬前置
- **均不阻塞 LW-PL-006 第二步（反向蒸馏）**：反向蒸馏管线可同步启动，图谱修复是并行轨道

## 五、纪律提醒（写进任务单）

1. 关系型边（同层 hub 互引）不得进豁免清单——这是上一轮例外规则裁定的红线，本批直连全是关系型边
2. 只动 related/Synthesis 一行级改动，不重写既有卡正文（#155 起的老规矩）
3. 全称量词申报（"全部双向闭合"）须附 grep 输出原文（T3 已立）

---

*欧阳锋 2026-07-12 · 诊断毕，编排请王语嫣裁定*
