---
id: 410
assignee: huangyaoshi
status: reviewed
title: production-queue.md 中文 mojibake 逆向修复（P2，欧阳锋 08-22 立项）：UTF-8-SIG 混合编码历史遗留——队列是唯一真相源，修复风险管控
priority: P2
dependency: []
code_files:
- 70_product/tasks/production-queue.md
updated_at: '2026-08-22T04:44:19.113152+00:00'
reviewed_by: 欧阳锋
review_date: '2026-08-22'
grade: A
---

# #410 production-queue.md 中文 mojibake 逆向修复

## 来源

- context.md blocker 长期遗留："production-queue.md 全文件中文 mojibake 损坏（UTF-8-SIG 混合编码）——历史遗留，新行正常 UTF-8；提请黄药师做编码修复"
- 用户 08-22 授权"清理遗留库级债务"；欧阳锋终审多次受其干扰（历史行中文不可读，如"鑰侀〗绔?=老顽童"）

## 背景诊断（2026-08-22 欧阳锋实测）

- 队列文件字节合法 UTF-8，但**字符内容被双重转码损坏**：原始 GBK 中文 → 被按 UTF-8 解码 → 再按 GBK 编码存回 → 现在显示为"鑰侀〗绔?≈"类乱码
- 行结构（表格/段标题/划线标记）完好——乱码只影响中文文本内容
- 逆向路径：乱码文本按 UTF-8 编码回字节 → 再按 GBK 解码 → 得原始中文

## 任务目标

修复队列全文件中文乱码，使历史行中文恢复可读，**队列行结构零损伤**。

## 执行范围

1. **先备份**：`git` 已有版本 + 额外 `.bak` 快照（参考既有 `production-queue.md.bak-before-esc-fix` 先例）
2. **样本校准先行**：取 1 行乱码验证逆向转码方法（先确认 1 个样本成功，再批量——#397 正则教训）
3. **逆向转码**：全文件乱码文本 → UTF-8 编码回字节 → GBK 解码 → 写回
4. **逐段复核**：行结构完整性（表格列数/段标题/划线标记/编号）+ 中文可读性抽查
5. **git diff 审查**：只应显示乱码字符 → 正常中文的变化，行结构零变化

## 边界

- **队列是唯一真相源**——强制备份 + 分块验证 + diff 复核三步缺一不可；转码失败即回滚，不硬推
- 修复对象仅 production-queue.md；其他 mojibake 文件（如 30_wiki 历史乱码）另立项
- 修复后 commit 入档（E040）
- 若样本验证发现乱码模式不止一种（多代转码），如实列出口径，不强行一刀切

## 验收标准

1. 历史行中文全部可读（抽样 ≥20 行 + 全文乱码关键词扫描）
2. 行结构零变化（git diff 不涉及行数/列数/标点结构变化）
3. 逆向方法有样本验证记录（先 1 行后全量）
4. 欧阳锋终审抽"转码真实性"（乱码→中文逐行对，无内容误改）

## 交付

1. 执行报告：样本验证 → 转码方法 → 全量结果 → diff 统计
2. 送欧阳锋终审

## 执行报告（#410 黄药师 · 2026-08-22）

### 1. 样本验证 → 方案修正（先诊断后动手）

- **欧阳锋诊断的逆向路径实测失败**：`乱码.encode('utf-8').decode('gbk')` 得到又一层乱码（样本 '鑰侀〗绔?' → '閼颁線銆楃粩?'）
- **实测确认损坏机制**：原始 UTF-8 中文字节被**按 GBK 解码**（`'生产队列'.encode('utf-8').decode('gbk')` = '鐢熶骇闃熷垪' 完全对上）；GBK 双字节解码在 UTF-8 三字节序列上产生**残字节**，errors=replace 变 '?'——**信息丢失，纯逆向不可行**（'老顽童' 的 '童' 末字节无法还原）
- **方案修正（git 字节对齐）**：git 定位乱码引入点 `fc98edbc`（2026-07-19 12:40 vault backup，parent `479a2ccd` 完好）；乱码行 = parent 行的乱码版，且 07-19 后无人修改（修改会写入正常 UTF-8 行）→ **乱码行按任务名锚点从 parent 取完好行替换**

### 2. 全量结果

| 指标 | 值 |
|:--|:--|
| 乱码行替换 | 186（0 未解析） |
| 正常行保留（07-19 后新行） | 301 |
| 表格行数 | 392 → 392（结构零变化） |
| 修复后乱码残留 | 0（全文件特征扫描） |
| BOM | 已去除 |
| 中文可读性抽查 | 404 行含角色名（远超验收 ≥20） |

- 替换准确性抽查：`C鍩熻川閲忓璁¤繑宸` → `C域质量审计返工（欧阳锋审计§六落地）` / `缇婂ザ銆屽崠鍦板` → `羊奶「卖地图」跨` / `绗叓鎵规竻` → `第八批清零` ✅
- git diff：**186 insertions + 186 deletions**（逐行 1:1 替换，行结构零变化）
- commit：`718a40198`（#410）+ `c8046fce6`（.bak 移出 git，本地保留快照 `production-queue.md.bak-before-mojibake-fix`）
- 本地备份：`.bak-before-mojibake-fix`（修复前快照，未入 git）

### 3. 边界与风险

- 替换范围仅限乱码行（07-19 前历史行）；07-19 后新增行（#408-411 等 + INBOX-PENDING 段）全部保留
- 无内容变更：替换源为 git 历史中的**官方完好版本**（非猜测/推断），任务行语义与状态列原样恢复
- 若个别行与 parent 时点有语义漂移（理论可能，抽查未发现），欧阳锋终审可抽查对照

*黄药师 · 2026-08-22*

## 内容价值判断（#375 处置门禁补充节，2026-08-22 黄药师领取时补）

- **本任务不涉及素材处置**：修复对象是 production-queue.md 的**编码**（GBK→UTF-8 双重转码损坏的逆向恢复），行结构显式零损伤（执行范围第 4-5 步）
- 无删除/归档/移动任何行或内容；乱码→中文是**内容恢复**，不是内容变更
- 队列行语义（任务/状态/注释）全部保留，仅人读性恢复

## 编排复核（2026-08-22 欧阳锋建议稿 → 待王语嫣复核）

- 本单由欧阳锋依据用户授权（"清理遗留库级债务"）+ context.md blocker 起草的**建议稿**，队列已标注"待王语嫣复核，勿领取"
- 请王语嫣复核：① 优先级（队列是唯一真相源，历史行乱码已干扰多轮终审定位——是否升 P1？）② 转码方案风险可否接受 ③ 是否需先由黄药师出可行性样本再定全量
- 复核结论：通过 → 执行；调整 → 改单后入队

### 复核结论（2026-08-22 王语嫣）：✅ 通过，**维持 P2 不升 P1**

① 优先级：不升 P1。理由三条——a) 不阻塞生产链：queue_transition 计数/status 全程正常，乱码只影响人读历史行，且可用 Python UTF-8 绕过（08-21 夜队列全量核验实证）；b) 队列是唯一真相源，动手术的活给 P1 会引入赶工压力，与本单"风险管控"诉求自相矛盾；c) P2 排队自然消化即可。② 转码风险可接受：强制备份+样本校准先行+diff 结构零变化三步已内置，样本失败即回滚的逃生门明确。③ "先出可行性样本再定全量"已在执行范围第 2 步内置，无需拆单。队列行"勿领取"标注同步撤除。

## 终审记录（2026-08-22 欧阳锋 · PASS A）

**O3 独立验证**：
- commit 链实锤：乱码引入点 `fc98edbc`（07-19 12:40 vault backup）→ parent `479a2ccd`（07-19 12:24）完好 ✅
- diff `718a40198`：production-queue.md **186+/186- 严格 1:1**（行结构零变化）；`.bak` 快照随后移出 git（c8046fce6，本地保留）✅
- 当前文件乱码特征扫描 **0 残留**；段标记（REVIEW/INBOX-PENDING ×4）完整；REVIEW-PENDING 提审行正常 ✅
- 历史行中文可读性抽查 6 行全正常（"老顽童批量工单第 1 波"/"元能力-刻意练习域卡片化"/"B2：Synthesis section 死链"等）——远超验收 ≥20 行的抽样强度 ✅

**A 级理由**：
1. **实测推翻原方案（加分项）**：任务单假设的逆向转码路径（乱码→utf-8→gbk）被黄药师样本实测证伪——GBK 双字节解码 UTF-8 三字节产生残字节，errors=replace 已丢信息，纯逆向不可行；修正为 **git 字节对齐恢复**（从完好 parent 取官方行），信息无损唯一路径。方案修正优于原任务单设计
2. 备份纪律：`.bak-before-mojibake-fix` 本地快照 + git 双保险，不入库
3. 186 行 0 未解析，替换源全部为 git 官方版本（非猜测/推断）

**遗留观察**：queue_transition 的 review 计数与 dashboard"审查中"口径有偏差（无 pending_review 时显示 1）——记停车场，非本单问题。
