---
id: 442
assignee: huangyaoshi
status: queued
updated_at: '2026-08-23T03:15:47+00:00'
---
# #442 负向门禁强词误伤返工（#435 审计返工）

- **任务号**：#442
- **状态**：queued
- **assignee**：huangyaoshi（基建单一实例；复审=欧阳锋；编排=王语嫣）
- **优先级**：P1（风清扬第三方审计发现 P1 误伤缺陷；老朱 2026-08-23 指令「落返工单，黄药师删词补反例」）
- **立项**：2026-08-23 王语嫣（审计意见 `diag_20260823_fengqingyang-gate-435-audit.md` 复核采纳；按协议 4 不动已审 #435，另开返工单）

## 任务目标

修复 #435 引入的强词子串误伤：`为空`/`空值` 在 `NEGATIVE_CLAIM_STRONG` 做 `w in text` 子串匹配，`"为空" in "不为空"` = True，导致「字段不为空」「确认非空值」这类**正向核查声明**被硬拦——审查者被逼给正向声明叠 `**存在性核查**` 锚点，属门禁自伤。

## 范围（按审计修正口径，一行级）

1. **STRONG 词表删除** `为空`、`空值`（10 词 → 8 词），不再做子串硬拦。
2. **正则 `NEGATIVE_CLAIM_PATTERN_DATA` 不动**——该正则已含「为空/空值」异常词且要求主语紧邻（0-3 字符）：
   - `grade 为空` → 正则命中，**仍拦** ✅
   - `字段不为空` → 不命中（「不」字隔断主语与异常词），**不再误伤** ✅
   - `值为空` → 命中仍拦；`值非空` → 不命中 ✅
3. **测试补否定式反例**：新增「不为空 / 非空值」反例用例（反测不误伤），并复测正例「grade 为空 / 值为空」仍拦——#435 的 +5 回归只测了「无截断/无损坏」正向声明，未测否定式（「不为空」对「为空」的否定），本单补上这一族。

## 验证

- 正测：「grade 为空」「值为空」无锚点 → 仍被拦（正则路径）。
- 反测：「确认 grade 不为空」「字段不为空，数据完整」→ 不拦、不误伤。
- 回归：`pytest 90_control/scripts/tests/test_queue_transition.py` 全过（28 原有 + 新增）。

## 边界

- 只删两词 + 补反例，不改正则、不动 #433/#435 其他已审内容、不扩新词（词表演进仍走「先小后大 + friction 观察」既定路径）。
- 交付五字段（F-034）+ 审查意见落盘（F-035）+ commit 入档。

## 关联

- 审计意见：`60_feedback/diagnosis/diag_20260823_fengqingyang-gate-435-audit.md`（老朱指令第三方审计；8 组独立用例 + 根因定位 + 三方缺陷如实标注）
- 被返工单：#435（reviewed PASS A-，冻结不动——缺陷属实现层，终审 5 例未覆盖否定式反例）
- 同族教训：子串自伤家族（#435「无截断」误伤同型）；「不核对就下判断」根治线（#433/#435 演进）
