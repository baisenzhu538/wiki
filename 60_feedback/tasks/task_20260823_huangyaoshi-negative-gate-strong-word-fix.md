---
id: 442
assignee: huangyaoshi
status: reviewed
updated_at: '2026-08-23T03:35:51.375076+00:00'
reviewed_by: 欧阳锋
review_date: '2026-08-23'
grade: A-
---
# #442 负向门禁强词误伤返工（#435 审计返工）

- **任务号**：#442
- **状态**：queued
- **assignee**：huangyaoshi（基建单一实例；复审=欧阳锋；编排=王语嫣）
- **优先级**：P1（风清扬第三方审计发现 P1 误伤缺陷；老朱 2026-08-23 指令「落返工单，黄药师删词补反例」）
- **立项**：2026-08-23 王语嫣（审计意见 `diag_20260823_fengqingyang-gate-435-audit.md` 复核采纳；按协议 4 不动已审 #435，另开返工单）

## 任务目标

修复 #435 引入的强词子串误伤：`为空`/`空值` 在 `NEGATIVE_CLAIM_STRONG` 做 `w in text` 子串匹配，`"为空" in "不为空"` = True，导致「字段不为空」「确认非空值」这类**正向核查声明**被硬拦——审查者被逼给正向声明叠 `**存在性核查**` 锚点，属门禁自伤。

## 范围（按审计修正口径，一行级）

1. **STRONG 词表删除** `为空`、`空值`（10 词 → 8 词），不再做子串硬拦。
2. **正则 `NEGATIVE_CLAIM_PATTERN_DATA` 不动**——该正则已含「为空/空值」异常词且要求主语紧邻（0-3 字符）：
   - `grade 为空` → 正则命中，**仍拦** ✅
   - `字段不为空` → 不命中（「不」字隔断主语与异常词），**不再误伤** ✅
   - `值为空` → 命中仍拦；`值非空` → 不命中 ✅
3. **测试补否定式反例**：新增「不为空 / 非空值」反例用例（反测不误伤），并复测正例「grade 为空 / 值为空」仍拦——#435 的 +5 回归只测了「无截断/无损坏」正向声明，未测否定式（「不为空」对「为空」的否定），本单补上这一族。

## 验证

- 正测：「grade 为空」「值为空」无锚点 → 仍被拦（正则路径）。
- 反测：「确认 grade 不为空」「字段不为空，数据完整」→ 不拦、不误伤。
- 回归：`pytest 90_control/scripts/tests/test_queue_transition.py` 全过（28 原有 + 新增）。

## 边界

- 只删两词 + 补反例，不改正则、不动 #433/#435 其他已审内容、不扩新词（词表演进仍走「先小后大 + friction 观察」既定路径）。
- 交付五字段（F-034）+ 审查意见落盘（F-035）+ commit 入档。

## 关联

- 审计意见：`60_feedback/diagnosis/diag_20260823_fengqingyang-gate-435-audit.md`（老朱指令第三方审计；8 组独立用例 + 根因定位 + 三方缺陷如实标注）
- 被返工单：#435（reviewed PASS A-，冻结不动——缺陷属实现层，终审 5 例未覆盖否定式反例）
- 同族教训：子串自伤家族（#435「无截断」误伤同型）；「不核对就下判断」根治线（#433/#435 演进）

## 内容价值判断（#375 处置门禁补充节，2026-08-23 黄药师领取时补）

- **本任务为代码修正**：STRONG 词表删 2 词（代码字符串，非素材）+ 测试用例更新——零素材处置
- 无删除/移动/归档动作；PROTOCOL §7 不触发

## 执行报告（2026-08-23 黄药师）

**完成内容**：负向门禁 STRONG 强词误伤返工——删「为空/空值」子串词（10→8），改由断言句式正则检测，补否定式反例回归。

**交付物**（改动文件清单）：
1. `90_control/scripts/queue_transition.py`：`NEGATIVE_CLAIM_STRONG` 删「为空」「空值」（子串误伤"不为空/非空值"正向声明，#435 审计）；PATTERN_DATA 不动（主语表不含"不/非"，天然不命中否定式）
2. `90_control/scripts/tests/test_queue_transition.py`：+3 回归（值为空正测 / 字段不为空反例 / 非空值反例）

**验证**（命令+输出）：
- `pytest test_queue_transition.py` → **31 passed**（28 原有 + 3 新增）
- 正测：「grade 为空」「值为空」→ **仍拦**（断言句式正则：主语+0-3 空格+为空）
- 反测：「字段不为空」「非空值」→ **完全不误伤**（ok=True 且 msg 为空——STRONG 已删子串 + 正则主语不匹配"不/非"）

**未做项**：正则未动（验证天然免疫否定式）；未判核查质量；未碰 #426/迁移/设计域

**需要谁动作**：欧阳锋复审本单（抽「否定式反例」——不为空/非空值不误伤）

---

## 终审记录（欧阳锋 · 2026-08-23 · 复审轮）

**结论：PASS / A-**

**背景认领**：#435 终审 5 例正反测未覆盖否定式反例——"为空" in "不为空" 子串误伤（STRONG 硬拦正向声明=门禁自伤）是**我的漏检**；风清扬第三方审计（8 组独立用例+根因定位）正确，返工路径合规（不动已审 #435，另开返工单）。

**O0 逐条溯源**：
1. **修复精准** ✅：STRONG 10→8 词（L614 实测删「为空/空值」）；PATTERN_DATA **不动**——判断正确：主语表不含"不/非"，"不"字天然隔断主语与异常词（"字段不为空"不命中），正则路径保留正测能力
2. **否定式反例族独立复现**（O3，直接调 `_check_negative_claims`）✅ 5 例全符合预期：`grade 为空`/`值为空`→仍被拦（正则路径）；`字段不为空`/`非空值`→零误伤（ok=True 且无提示）；带核查锚点→放行
3. **测试独立复现** ✅：pytest **31 passed**（28 原有 + 3 新增：值为空正测/字段不为空反例/非空值反例）
4. **边界** ✅：只删两词+补反例；未改正则/未动 #433/#435 其他内容/未扩新词；commit 39339249f 入档

**发现问题**：
- 🟠 词表演进机制缺「否定式反例族」前置测试规范：本次误伤源于「子串匹配 + 未测否定式」——建议 friction-log 记一条：词表新增词必须带否定式反例用例（`不为X`/`非X`/`无X` 三族）才算入表

**魔鬼代言人**：3 个月后最可能出问题——后续扩表（如"不完整"）再犯子串自伤（"不完整" in "完整"？不——"不完整"是新词无歧义；但"完整"进表会误伤"不完整"）——否定式前置测试规范可防

**存在性核查**（本意见书负向断言证据）：
- 「STRONG 删词」→ 核查：L614 词表实测 8 词（不存在/未备份/未同步/确认缺失/缺失/卡住/丢失/死锁），"为空/空值"已出表
- 「否定式零误伤」→ 核查：`字段不为空`/`非空值` 直接调函数实测——ok=True 且 msg 空（上方 5 例输出）
- 「正则未动」→ 核查：git show 39339249f --stat + 词表/正则行 diff 仅 STRONG 行变化

**残余风险**：词表扩表演进仍靠 friction 观察；否定式前置测试规范记 friction-log 待固化。

*欧阳锋 · 2026-08-23 · 复审 PASS A-*
