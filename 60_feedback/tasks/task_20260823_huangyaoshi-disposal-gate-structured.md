---
id: 457
assignee: huangyaoshi
status: in_progress
updated_at: '2026-08-23T05:53:47.869604+00:00'
version: v0.1
instance: huangyaoshi
---
# #457 处置类门禁判定结构化（误判两连发 + 可绕过漏判根治）

- **任务号**：#457
- **状态**：queued
- **assignee**：huangyaoshi（基建单一实例；终审=欧阳锋；编排=王语嫣）
- **优先级**：P1（老顽童生产实测两连发 #189/#454 + 门禁可绕过漏判——PROTOCOL §7 素材删除禁令防线）
- **立项**：2026-08-23 王语嫣（老顽童建议书 `diag_20260823_laowantong-disposal-keyword-misjudgment.md` 采纳：方案 1+2 组合）

## 缺陷（建议书已定位，queue_transition.py L383/L395）

- 全 body 子串匹配 + 关键词黑名单过宽：任务单术语（「删除旧口径」「已废弃」）被误判为素材处置动作 → 生产任务被卡（#189/#454 两连发，同义改写绕过）
- 反向更危险：真处置任务用「作废/淘汰/下线/回收」等词即可规避门禁——黑名单天然可绕过，素材删除禁令（逐件老朱亲批）形同虚设

## 方案（方案 2 为主 + 方案 1 组合）

1. **显式标记优先（方案 2，主）**：处置类任务 frontmatter 必带 `disposal: true`（王语嫣编排侧写任务单时标注——含处置动作的任务单模板加此字段）；关键词命中降级为**提示**（输出「疑似处置未标记，请确认」）不做硬判定
2. **限定检查范围（方案 1，辅）**：关键词检查只扫「## 动作」节+任务目标节，不扫全 body——正文叙述「删除 X 口径」不触发，动作节「删除素材文件」仍提示
3. **回归用例**：#189/#454 两个误判场景（放行）+ 真处置正测（标记触发内容价值判断门禁）+ 未标记真处置（提示不硬拦，提示信息引导补标记）——正反测齐

## 验证（验证分层声明）

- L1：单测四场景；L2 狗粮：#454 同款任务单 claim 走通（无误拦）；L3 待活体：下一张含处置词的正常任务单零摩擦

## 边界

- 不动 #375 门禁意图（素材删除禁令不变）；与 #444（frontmatter 口径）风格一致；PROTOCOL §7 不改

## 执行报告（F-034 五字段+验证分层声明，complete 前必填）

（生产者填写）
## 执行报告（2026-08-23 黄药师）

**完成内容**：处置类门禁判定结构化——显式标记 `disposal: true` 优先（硬门禁）+ 关键词限定范围降级为提示（#189/#454 误判根治，PROTOCOL §7 防线不弱化）。

**交付物**（改动文件清单）：
1. `90_control/scripts/queue_transition.py`：`_check_disposal_gate` 重构——`disposal: true` frontmatter → 强制「内容价值判断」节（硬拦）；`_extract_action_scope` 限定关键词检查范围（只扫「## 动作」+「## 任务目标」节，正文叙述不触发）；关键词命中降级为提示（"疑似处置未标记，请补 disposal: true"）不硬拦
2. `90_control/scripts/tests/test_queue_transition.py`：+5 回归（#189/#454 误判放行 / disposal:true 无节硬拦 / 有节通过 / 未标记真处置提示）

**验证**（命令+输出）：
- L1：pytest **41 passed**（36 原有 + 5 新增）；正反测齐——#189「清理」在背景叙述→零提示放行；#454「已废弃」在背景节→零提示；disposal:true 无内容价值判断节→硬拦；真处置动作节命中→提示不硬拦（引导补标记）
- L2 狗粮：#454 同款任务单格式走通（单测场景即真实格式）；「## 背景」等非动作节天然隔离（节边界验证）
- L3 待活体：下一张含处置词的正常任务单零摩擦（老顽童生产实测）

**未做项**：
- 未改 #375 门禁意图（素材删除禁令不变）；PROTOCOL §7 未动；关键词表未扩（维持 7 词，误报进 friction）
- 编排侧任务单模板加 `disposal: true` 字段（王语嫣侧动作，本单代码就绪）

**需要谁动作**：
- 王语嫣：含处置动作的任务单写 frontmatter `disposal: true`（模板加字段）
- 老顽童：#189/#454 场景重新走 claim 应零摩擦（L3 验证）
- 欧阳锋：终审本单（抽「显式标记优先/限定范围/意图不变」）
