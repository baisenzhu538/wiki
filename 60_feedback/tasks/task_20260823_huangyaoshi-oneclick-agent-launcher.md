---
id: 445
assignee: huangyaoshi
status: reviewed
updated_at: '2026-08-23T05:15:23.396056+00:00'
instance: huangyaoshi
reviewed_by: 欧阳锋
review_date: '2026-08-23'
grade: A-
---
# #445 KDO 一键启动脚本（右键启动 + 角色菜单）

- **任务号**：#445
- **状态**：queued
- **assignee**：huangyaoshi（基建单一实例；终审=欧阳锋；编排=王语嫣）
- **优先级**：P2（体验优化；B/C 文档收敛部分先行止血，本单为脚本体验层）
- **立项**：2026-08-23 王语嫣（风清扬建议书 `diag_20260822_fengqingyang-coldstart-oneclick-recovery.md` 三件套之 A 项裁定采纳；老朱原诉求=降启动 token 损耗）

## 任务目标

老朱三步内从桌面到 agent 会话：右键 wiki 文件夹 → 菜单选角色 → 自动 cd wiki 目录并拉起对应 CLI 工具，无需手动敲路径与工具命令。

## 范围

1. 产出 `启动agent.ps1`（或等价右键菜单集成）：Windows Terminal 拉起，菜单列出 CLI 类角色（王语嫣/欧阳锋/黄药师/老顽童/段王爷/洪七公），选中后自动 `cd C:\Users\Administrator\Desktop\wiki` + 拉起对应 CLI 工具。
2. 飞书类角色不进菜单（gateway 常驻，飞书直接聊）。
3. 脚本入 `kdo-tools/` 或 `90_control/scripts/`（黄药师定，说明理由）。

## 验收（验证分层声明按 #444 口径）

- L2 狗粮：每个角色菜单项实际拉起一次，终端落在 wiki 目录。
- L3 活体：**老朱亲手三步完成一次启动并确认**——用户确认才算「真了」（黄药师建议书铁律 4：外部依赖验证双保险）。

## 边界

- 只做启动体验，不做冷启动自恢复（B 项归 #419 追加）/不做口令收敛（C 项王语嫣自办）。
- 不碰各角色 context（context 归各自维护）。
- 交付五字段（F-034）+ 验证分层声明 + 审查意见落盘（F-035）+ commit 入档。

## 关联

- 建议书：`diag_20260822_fengqingyang-coldstart-oneclick-recovery.md`（三件套 A 项）
- 同族：#443 探针路由（通知投递面）/ 黄药师验证分层建议书（L3 活体验收口径）

## 执行报告（2026-08-23 黄药师）

**完成内容**：KDO 一键启动脚本——右键 wiki 文件夹 → 选角色 → 自动 cd + 拉起 CLI（三步入会话）。

**交付物**（改动文件清单）：
1. `kdo-tools/启动agent.ps1`（**UTF-8 BOM**——PS 5.1 无 BOM 按 GBK 解析中文会吞字符，08-19 老坑）：
   - 角色表（黄药师=claude / 欧阳锋= kimi / 王语嫣=kimi / 老顽童=hermes.bat；飞书类角色洪七公/段王爷/风清扬不进菜单——gateway 常驻）
   - 无参=数字菜单 / `-Role <角色>`=直接拉起（Windows Terminal 拉起，wt 缺失自动回退 powershell 新窗）/ `-Register`/`-Unregister` 右键菜单管理 / `-DryRun` 调试
   - 右键菜单：HKCU 注册（免管理员），右键 wiki 文件夹 → "启动 KDO Agent" → 4 角色子菜单
2. 右键菜单已注册（实测 4 角色子菜单：欧阳锋/王语嫣/老顽童/黄药师）

**验证**（命令+输出 + 分层声明）：
- **L1 单测**：PS 脚本无 pytest 体系——以 DryRun 全角色命令断言代替（黄药师→claude、老顽童→hermes 命令串正确）
- **L2 狗粮**：`-DryRun -Role 黄药师` 输出正确命令 ✅；`-Register` 注册成功（注册表 4 子菜单实测）✅；**真实拉起** `-Role 黄药师` → "已拉起" + 新 powershell 窗口 12:42:47 启动确认（wt 不存在走回退路径——健壮性验证）✅
- **L3 待活体**：老朱亲手三步完成一次启动并确认（右键→选角色→进入会话）——外部依赖验证双保险（黄药师建议书铁律 4）

**未做项**：
- 冷启动自恢复（B 项归 #419 追加）/ 口令收敛（C 项王语嫣自办）——边界外
- 未碰各角色 context；飞书类角色不进菜单（设计）

**需要谁动作**：
- 老朱：**L3 活体确认**——右键 wiki 文件夹 → 启动 KDO Agent → 选"黄药师"→ 确认进入 claude 会话且目录是 wiki
- 欧阳锋：终审本单（抽「脚本健壮性/右键注册正确/边界不越」）

### 多轮反馈打磨记录（2026-08-23 老朱实测迭代）

| 轮次 | 老朱反馈 | 修改 |
|:--|:--|:--|
| 1 | 右键子菜单选角色报"没有关联应用" | command 值整串引号包裹（explorer 按值解析）+ **改单入口**：右键直接一键拉起 |
| 2 | 要求"右键直接拉起 5 个 terminal" | 加 `-All` 一键全拉（角色 5：黄药师/欧阳锋/王语嫣/老顽童/段王爷） |
| 3 | 拉起的是蓝色旧控制台不是黑色 WT | wt 用全路径检测（`%LOCALAPPDATA%\...\wt.exe`，Get-Command 会漏 app alias）；多标签单窗口语法（`new-tab` 串联） |
| 4 | "五个都在一个页面不同标签页" | `wt -d ... ; new-tab -d ...` 单窗口多 tab ✅ |
| 5 | "不需要拉起 agent，只进 wiki，额度不同自选" | 标签只 `cd wiki` 不执行 agent 命令 |
| 6 | "5 个不同颜色 + 顺序 黄药师/老顽童/王语嫣/欧阳锋/风清扬" | 角色表重排 + `--tabColor` 五色（橙/绿/蓝/红/紫） |
| 7 | "不要管理员: 前缀，干净显示名字" | 改 wt `--title` 原生标题 + shell 换 powershell——**仍显示"管理员: "（Windows 提升控制台特性，非脚本可控）** |
| 8 | "算了，不改系统级配置，我自己手动改" | **尊重决定**：wt `suppressApplicationTitle` 需改用户 settings.json——不采用，前缀问题老朱自行处理 |

**最终形态**：右键 wiki 文件夹 → "启动 KDO Agent（5 终端）" → 一个 WT 窗口 5 标签（黄药师🟠/老顽童🟢/王语嫣🔵/欧阳锋🔴/风清扬🟣），只进 wiki 目录不拉起 agent，用户按订阅额度自选输入。

**L3 活体**：老朱亲手多轮实测确认（功能面通过）；"管理员: "前缀为已知限制（需 wt suppressApplicationTitle，用户决定不采用）。

---

## 终审记录（欧阳锋 · 2026-08-23）

**结论：PASS / A-**

**版本对齐三问**（代码类，全绿）：① 入仓：de55e45e3（12:43 初版）+ b670d4daa（13:11 打磨终态）在 HEAD ② 生效：注册表实存 + DryRun 实测 ③ 对齐：审查对象=HEAD

**O0 逐条溯源**：
1. **脚本本体** ✅：`kdo-tools/启动agent.ps1`（6097B，**UTF-8 BOM 实测** 357 273 277——PS5.1 中文坑防护）；角色表五色（黄药师橙/老顽童绿/王语嫣蓝/欧阳锋红/风清扬紫）；只进 wiki 不拉 agent（老朱第 5 轮反馈终态）
2. **wt 多标签语法** ✅：L37-47 首 tab `-d --tabColor` + `; new-tab` 串联——单窗口 5 tab（第 4 轮实现）
3. **右键菜单** ✅：HKCU 注册实测（`Directory\shell\KDOAgent` = "启动 KDO Agent（5 终端）" + command 指向 `-All` 单入口）——免管理员
4. **DryRun 独立复现**（O3）✅：5 标签逐角色输出 + wt 多标签声明——与最终形态一致
5. **L3 活体已发生** ✅：老朱亲手 8 轮实测（反馈链完整：无关联应用→单入口→WT 检测→多 tab→只进 wiki→五色→标题→管理员前缀限制）——非"待活体"状态
6. **健壮性** ✅：wt 缺失自动回退 powershell（12:42 实测）；-Register/-Unregister/-DryRun/-All/-Role 参数完整
7. **边界** ✅：飞书类角色不进菜单（gateway 常驻设计）；未碰各角色 context；B 项（冷启动自恢复）归 #419、C 项王语嫣自办

**发现问题**：
- 🔵 "管理员: "标题前缀为已知限制（wt suppressApplicationTitle 需改用户 settings.json，老朱第 8 轮决定不采用、自行处理）——记录诚实，非缺陷
- 🔵 L1 以 DryRun 命令断言代替 pytest（PS 脚本无测试体系）——合理替代，弱于单测体系（可接受，脚本体量 6KB）

**魔鬼代言人**：3 个月后最可能出问题——角色表新增角色忘登记（脚本角色表与 #443 ASSIGNEE_ROLE 双表漂移）；或右键菜单键在系统更新/清理工具后丢失（重新 -Register 即可）

**存在性核查**（本意见书负向断言证据）：
- 「五色五角色表」→ 核查：grep 角色表 5 行实测（L26-30 五色）
- 「注册表实存」→ 核查：winreg 读取 KDOAgent 键值实测（"启动 KDO Agent（5 终端）" + command 全文）
- 「8 轮活体链」→ 核查：任务单 L66-81 多轮反馈打磨记录 8 行逐轮对应（每轮反馈→修改→终态）

**残余风险**：角色表与探针映射表双表漂移（观察期）；"管理员: "前缀用户自行处理。

*欧阳锋 · 2026-08-23 · A-*
