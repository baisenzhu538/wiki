---
id: 476
assignee: wangyuyan
status: queued
updated_at: '2026-08-23T22:00:00+00:00'
version: v1.0
doc_id: D-20260823-024
---

# #476 队列状态核对纪律固化（禁用正则手搓，统一走 queue_transition 对账）

- **任务号**：#476
- **状态**：queued
- **assignee**：wangyuyan（编排维护权在王语嫣；若需工具化包装脚本归黄药师）
- **优先级**：P1（E051 犯错根因闭环——今天误报 #474 状态，用户纠正后才回源）
- **立项**：2026-08-23 王语嫣（用户令「立 #476 并登记错误模式库」；E051 已登记）

## 背景

E051：汇报看板时自写 python 正则从队列行长备注行抓状态列，#474 实际 reviewed 被误报成 queued/未领取。根因=用不可靠二次解析当结论，未回源核对权威数据。与 E041（核结构）/E038（核状态）/E017（正则跨字段误读）同族。

## 任务

### 任务 1 · 纪律固化（王语嫣自办，立即可做）

在编排纪律/context 铁律补三条（吸收 E051 解决方案）：
1. 报任何队列/任务状态前，以**任务单 frontmatter `status:` 字段**为权威真相
2. 队列行状态以 **`queue_transition.py` 对账输出**为准，禁手搓正则/grep 拼图当结论
3. 自写脚本只辅助不判决，报用户的结论必须回源核验

落点：`wangyuyan-context.md` 或 `task-orchestration` skill 启动检查清单（铁律区）；同步进启动恢复清单「报状态前核对」项。

### 任务 2 · 工具层约束（二选一，待裁定）

- **方案 A（王语嫣自办，轻量）**：写一个 `kdo queue-status <id>` 包装命令（调 queue_transition 对账），禁止任何人手搓正则读队列——文档/context 里写明「查状态只走此命令」
- **方案 B（黄药师，若要做硬门禁）**：给 queue_transition 加 `status-check` 子命令 + 在 lint/预审里检测「手搓正则解析队列行」的禁用模式（类似 #473 lint 思路）

> 今天规矩：若选方案 B 派黄药师，须先确认他未领取同类单才能写；#474 已 reviewed 不冲突。本单默认方案 A（王语嫣自办，不占黄药师），方案 B 留作后续演进候选。

## 验证（验证分层声明）

- L1：纪律写入 context/skill 可 grep 校验
- L2 狗粮：下次报看板状态，先跑 queue_transition 对账 / 读 frontmatter，不再手搓正则——对照 E051 复发检查
- L3 待活体：连续 10 次状态汇报零误报（观察期）

## 边界

- 只读纪律固化 + 轻量包装；不动队列状态机（#472/#453 已建工具复用）
- 方案 B 若立需新单，不塞本单（上板冻结，E047 同律）

## 执行报告（F-034 五字段+验证分层声明，complete 前必填）

（生产者填写）
