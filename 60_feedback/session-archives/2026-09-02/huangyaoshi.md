---
session_id: huangyaoshi-2026-09-02
agent_id: huangyaoshi
date: 2026-09-02
created_at: 2026-09-02T14:44:42.027218+00:00
updated_at: 2026-09-02T14:44:42.027218+00:00
git_head: 96b1853e1
content_hash: 474cbf072b38
---

# huangyaoshi · 2026-09-02

# 黄药师 daily-context 2026-09-02（夜班施工链 03:00–08:20）

## 差异栏（第 1 章）

本次 vs 上次（09-01）：**首次在「等终审」窗口里系统化做下一单的只读预勘察**——过去审查等待=空转，本次把 #604/#605/#607/#608 的引用对账/机制勘察全部塞进等待窗，解禁后即秒施工。复发模式：**git 移动文件只看 add 侧漏掉删除侧**（#605 首轮 FAIL）——与 P-29/P-30 批量家族同根，「批量动作后的 git status 双向核验」仍是弱点。被打破的假设：以为「vault backup 有系统级调度」——实际一直是会话级 cron 扛着，重启即死。

## 概要

夜班五连单全闭环：#603 tmp 脚本清理（66 归档+3 凭据脚本隔离）→ #604 散点归位（假盘符树/Harness/mp4）→ #605 dispatch 收口（扫描面白名单+台账停发）→ #607 backup 停摆根治（系统级 S4U 调度+探针第十信号）→ #608 image_detail 死循环修复；含 #605 一轮 FAIL 返工。

## 关键决策

| 决策 | 理由 | 结果 |
|:--|:--|:--|
| `_tmp_skill_health.json` 迁 90_control/baseline/ 而非 _tmp/ | 任务单二选一，baseline 是正式引用应去的稳定面 | 建议书引用链不断，终审 PASS |
| 凭据读取脚本随 #600 口径入隔离区而非归档 | #600 已定「凭据类移出 vault 跟踪面」 | 3 件 git rm --cached + 隔离区 |
| tmp_video/ 与 _tmp_m371_domain_unknown.txt 列例外保留 | 前者被已发布 manifest 路径引用，后者被 clean-metadata-371.py 写引用 | 例外清单随报告交付，终审认可 |
| #605 扫描面用白名单（顶层+pending-cards）而非黑名单扩列 | 王语嫣裁定原文即白名单口径；白名单结构性防新大目录再洪水 | 扫描面 7908 行级→371 文件 |
| #607 根因不定到具体会话名，按「会话级 cron」定类修复 | 旧会话 cron 注册表随会话死亡不可得；排除法+事件日志+节拍相关性已足够支撑修复方向 | 系统级 S4U 任务上线，当拍自证产 commit |
| schtasks 用 cmd 包装而非 XML 内联参数 | 内联引号参数 S4U 下 0x80070002 FILE_NOT_FOUND | cmd 包装（role-clock 同构）一次过 |

## 思维盲点

1. **「mv 完成=交付完成」**：#605 移 49 份台账后只看隔离区落位，没跑 `git status` 看删除侧——git 视角里删除未入仓=没发生。为什么漏掉：物理移动的完成感覆盖了版本轨动作，与 B1「写完文件≠入队」同构——机械收尾步骤无成就感就被跳过。
2. **ls-files 头部取样误判**：#605 用 `git ls-files <dir> | head -3` 看到 archive/ 子目录就推断顶层 dispatch untracked。为什么漏掉：head 截断的输出当了全量证据——取样偏差，该用 `git ls-files <dir> | wc -l` + 针对具体文件验证。
3. 差点把 Harness 连字符版当「规范版」保留——是先 grep 到 plan_20260621 引用全角版才定案。为什么差点漏：凭文件名直觉判断「规范」，没先查引用。

## 顿悟

- **等待窗不是垃圾时间**：串行队列（pending_review 禁领新单）表面是瓶颈，实际把「勘察」与「施工」天然分层——勘察全是只读动作，不占 claim 名额。夜班吞吐量的上限不在施工速度，在勘察有没有提前做完。
- 备份的心跳和备份本身必须不同层：会话级 cron 既扛备份又无法自报其死——停机探测必须挂在系统级探针上（第十信号），「谁死谁知道」的机制等于没有机制。

## 过程资产

- `kdo-tools/vault_git_backup.py` + `kdo-tools/vault-git-backup.cmd`（新增，系统级备份执行体）
- schtasks `kdo-vault-git-backup`（每 30min，S4U 后台模式）
- `kdo-tools/conveyor_probe.py` 第十信号 `_scan_backup_stall`（backup 停拍 >24h 报警）
- `kdo-tools/watch_inbox.py` 扫描面白名单化 + `DISPATCH_LEDGER_ENABLED=False`
- `kdo-tools/wechat_link_monitor.py` image_detail 前置 mark_seen 跳过
- `90_control/notification-coverage-matrix.md` 行 9 修订 + 行 24 新增（§3.19 同步，本晚零违例）
- `_tmp/603-archive/`（66 件）、`90_control/.sandbox/quarantine-20260902/`（假盘符树+Harness 连字符版+49 份 dispatch+3 凭据脚本）
- `_tmp/605-test/`、`_tmp/608-round1/2.log`（验证现场）
- 建议书引用修正：`60_feedback/diagnosis/建议书_20260901_skill健康度勘察与检测方法论.md` L146
- 工作 commits：eac1f1337 / e05395857 / 8590e4ecb+db6a93574 / cf4f371f3(+a5d0bee82,79895d44b) / 7d2e73837
- friction-log 1 行（#605 删除侧漏 add）

## 元反思

下次怎么做才能不一样：批量移动文件的收尾动作写成死流程——「mv 后必跑 `git status --porcelain -- <目录>`，D 行非零=交付未完成」，不靠自觉。等终审的轮询循环可以继续复用（32s 拍 + 状态翻牌即醒），但勘察清单要在 claim 前就建好，不只建下一单的。

## Truman复盘

### 逐轮映射

| 轮次 | 人做什么 | 双三角要素 | AI做什么 | 双三角要素 |
|:--|:--|:--|:--|:--|
| 老朱派夜班链（5 单定序+纪律四条） | 定方向定边界 | 方向 | 恢复上下文+按序施工 | 场景 |
| #603 清理范围含混（59 vs 实测口径差） | （任务单口径） | 方向 | 全量 grep 引用对账，区分功能引用 vs 叙事引用 | 判断 |
| #605 终审 FAIL（删除侧未入仓） | 欧阳锋独立复验抓漏 | 校准 | 返工补 commit+修报告误述 | 执行 |
| #607 根因不定 | 任务单给排查方向（schtasks/日志/事件） | 方向 | 事件日志 6005/6006 锁重启时刻+排除法定类 | 判断 |

### 飞轮效应

加速了「审计→立项→修复→机制化」回路：风清扬散点审计的 R5/R6/R7 当夜闭环，且 #607 不止修 backup，还把「停拍不可见」这个元问题变成了探针信号——下次同类停摆从 6 天缩到 24h 内自报。

### 对照实验

- 无人（纯 AI 夜班）：#604 假盘符树「是否含独有内容」、Harness 保留哪份，没有引用对账纪律就会拍脑袋删错。
- 无 AI（纯人）：6 天空窗的根因排查（事件日志+节拍相关性+排除法）人要翻一晚上；mp4 14 处引用改指手工做不动。
- 合在一起：人定边界和验收，AI 跑全量对账和机械施工——今晚 5 单的形态。

### 下次改进

- Agent 自身：把「批量移动→git status 双向核验（A 侧+D 侧）」写进施工肌肉，或给 batch 脚本加内置断言。
- 方法论卡：批量三问可加第四问候选——「移动类操作的版本轨两侧核验了没」（待王语嫣裁定是否入库）。

---

# 黄药师 daily-context 2026-09-02（晚班第二场 #618，约 21:30–22:07）

## 差异栏（第 1 章）

本次 vs 上次（同日夜班）：**首次把「修测试」修成「修产品」**——对齐 smoke 断言后暴露出 SQLite 迁移以来 derived_outputs 从未落库的真回归，测试修复只是入场券。复发模式：无（批量/git 核验家族本轮未触发）。被打破的假设：以为「顺序依赖 flake」真是顺序问题——无随机插件、顺序确定，真相是负载时序（Windows RST），「单跑过全量挂」的字面描述把根因方向带偏了。

## 概要

#618 测试 flake 治理两例闭环：test_cli_smoke 断言对齐 SQLite 现行 schema（+顺带根治 derived_outputs 持久化丢失）；test_dashboard_server flake 定位为 do_POST 早退不读请求体→Windows RST（WinError 10053）→修复后压力复测 6/6 过、全量回归 614 passed/1 skipped×5（含提交后终跑），commit db343f7，已提审待欧阳锋。

## 关键决策

| 决策 | 理由 | 结果 |
|:--|:--|:--|
| smoke 测试改走 `load_state` API 而非直接读 state.json | SQLite 迁移后 state.json 剥离 MVP_COLLECTIONS，JSON 不再是全量真相源 | 断言与现行 schema 对齐，KeyError 根治 |
| derived_outputs 断言不删除、修产品代码 | 删断言=掩盖 produce 链路真回归，终审必被打回 | artifacts.py 补 SQLiteCollection 回写，链路恢复 |
| dashboard flake 先造压力复现再下手（P-21） | 静态分析三轮无果；磁盘 IO 压力 5 连跑稳定复现 WinError 10053 | 根因锁死 RST，修复有实证对照（前 3/5 挂→后 6/6 过） |
| 修复落在服务端（早退前读请求体）而非放宽测试 timeout | 真实用户浏览器同受此 bug；timeout 是掩盖不是解耦 | 8 行改动根除全族 403/400 早退 RST |
| KDO 仓 3 文件 path-scoped commit 后 complete | E040 门禁「未 commit=未发生」 | 门禁过，pending_review |

## 思维盲点

1. **把「断言过期」当纯测试问题**：第一修完立刻撞上 derived_outputs 断言失败，差点就想把断言弱化交差。为什么差点漏：任务单字面是「断言对齐 schema」，完成感诱导往「改断言」方向收敛——实际现行 schema 的语义包含「produce 必须写回 derived_outputs」。
2. **盲信前人的 flake 定性**：「顺序依赖」四个字让我先查了两轮测试间污染（chdir/env/monkeypatch），全是无效路径。为什么漏掉：没先质疑定性本身—— pytest 无随机插件、顺序确定，「顺序依赖」在机制上不成立，该第 0 步就算这笔账。
3. 首次 complete 被 E040 门禁打回（交付物路径未带 KDO 仓全路径+未 commit）——门禁口径变更后第一次踩。为什么漏掉：凭记忆走旧流程，没把门禁报错当文档读。

## 顿悟

- **「单跑过全量挂」≠顺序依赖**：在没有随机顺序插件的套件里，这个症状的更优假设是「全量跑时机箱更热」——负载/IO/AV 扫描时序。定性词（顺序依赖）会锁死排查方向，复现前都该当作假设而非事实。
- **Windows socket 语义是测试 flake 的隐藏大户**：服务端 close 时接收缓冲有未读数据→RST→客户端 ConnectionAbortedError 吃掉已到响应。任何「早退不读 body」的 HTTP handler 在 Windows 下都是定时炸弹。

## 过程资产

- KDO 仓 commit `db343f7`：`tests/test_cli_smoke.py`（read_workspace_state helper）、`kdo/artifacts.py`（link_artifact_to_sources SQLite 回写）、`kdo/dashboard_server.py`（do_POST 早退前读请求体）
- `60_feedback/tasks/task_20260902_huangyaoshi-kdo-tests-flake-governance.md` 五字段执行报告（含全量回归原样输出 614 passed/1 skipped in 118.63s）
- 验证现场：磁盘压力复测脚本（/tmp 内联）、全量回归 4 连跑日志 /tmp/kdo_flake_loop.log
- 队列：#618 pending_review（E040 门禁通过后翻转）

## 元反思

下次怎么做才能不一样：接到「flake 治理」类任务，第 0 步先验定性词——查套件有无随机顺序插件、flake 报错原文，不带着建议书的定性开工。「修测试」任务默认多问一句：这个断言守护的链路现在还工作吗？（本次若不追问，derived_outputs 静默丢失会继续烂下去。）

## Truman复盘

### 逐轮映射

| 轮次 | 人做什么 | 双三角要素 | AI做什么 | 双三角要素 |
|:--|:--|:--|:--|:--|
| 老朱派 #618（指令含任务号+纪律四条） | 定边界定出口 | 方向 | 恢复上下文+claim+施工 | 场景 |
| 建议书定性「顺序依赖」（欧阳锋/王语嫣立项） | 给初始假设 | 方向 | 静态排查无果后造压力环境复现，推翻定性锁定 RST | 判断 |
| E040 门禁打回首次 complete | 机制代人来校准 | 校准 | 读门禁源码改交付物口径+commit 后重过 | 执行 |

### 飞轮效应

加速了「终审复跑→建议书→立项→根治」回路：#616 终审抓出的数字漂移，当夜变成两例 flake 的根治+一条提审纪律的首次实跑（原样输出贴报告）。副产品：SQLite 迁移的 derived_outputs 暗伤被顺路排掉。

### 对照实验

- 无人（纯 AI）：derived_outputs 断言大概率被弱化交差——任务单字面「断言对齐」给的完成感方向就是改测试；门禁 E040 是机制兜底。
- 无 AI（纯人）：WinError 10053 的 RST 机理+磁盘压力复现环境搭建，排查成本以天计。
- 合在一起：人给假设和校准机制，AI 跑复现实验推翻假设——本轮 flake 治理的完整形态。

### 下次改进

- Agent 自身：flake 任务开工先跑「定性词质疑三问」（有无随机插件？报错原文？字面症状的最简机制解释？）。
- 方法论卡候选：「单跑过全量挂」症状决策树——顺序插件检查→负载时序假设→压力复现（待王语嫣裁定是否入库）。

# 黄药师 daily-context 2026-09-02（晚班第三场 #619，约 22:20–22:42）

## 差异栏（第 1 章）

本次 vs 上次（同日晚场 #618）：**首次在「回补扫描面」任务里主动做存量基线播种**——不是改完就跑让 41 件历史文件洪水登记，而是先盘点 state 覆盖、按 mtime 切开新旧、把 12 件已被 wechat_promote 消费过的陈旧件播种为已见，只放 05:47 批次 6 件过闸。复发模式：无（E040 门禁本轮一次过——#618 踩过的「交付物先 commit 再 complete」已内化）。被打破的假设：以为任务单「05:47 四件」是精确计数——实测 mtime 05:47 共 6 件，任务单数字以实测为准在报告里更违。

## 概要

#619 watch_inbox 扫描面回补闭环：#605 裁剪把 wechat-collect/video_transcripts/video_transcripts_small 三个管线落点误裁出扫描面（05:47 批次静默漏登记实证）。修复=扫描面白名单化（SCAN_SUBDIRS 四目录常量）+ 白名单内跳过 `_` 前缀目录段与 knowledge/（promote 中间产物，case 卡另有 pending-cards 落点，防重复派发——原 EXCLUDE_DIRS 的防重意图下沉到目录内）。实跑 6 件全登记（看板+6、收件箱推送），重跑幂等零新增，回归 4 passed（含新增白名单用例），矩阵行 9 同步，commit d93853fcf，已提审待欧阳锋。

## 关键决策

| 决策 | 理由 | 结果 |
|:--|:--|:--|
| 白名单目录内跳过 knowledge/ 与 `_` 前缀段 | knowledge/=promote 中间产物且 case 卡另有 pending-cards 落点，扫了=重复登记（原 EXCLUDE_DIRS 排除整个 wechat-collect 的正是这个防重意图） | 首跑登记量从 41 压到 17，且无一件是管线内部产物 |
| 存量 12 件播种入 state 再实跑 | 11 件陈旧 src 均已被 promote 消费、case 卡在 pending-cards 已在册；不播种=11 条重复待编排冲王语嫣看板 | 实跑恰好只登记 05:47 批次 6 件，验证口径干净 |
| EXCLUDE_DIRS 死常量移除而非保留 | #605 后 scan 已不读它，与白名单并存=双口径漂移源 | 单一真相源=SCAN_SUBDIRS |
| 新增 test_scan_whitelist_subdirs 回归 | 扫描面是本次事故面，无测试=下次裁剪再误伤无门禁 | 4 passed，白名单内外+内部目录三路断言 |

## 思维盲点

1. **差点直接「改完就跑」**：第一反应是 whitelist 一加了事，41 件登记洪水（含 31 件管线内部产物）会在王语嫣看板炸开。为什么差点漏：任务单验证口径只写「4 件应被登记」，完成感诱导直奔验收，没想首跑的存量副作用。是 #605 自己的反面教材（863KB 巨件）把我拉住——先跑 state 覆盖盘点脚本再动手。
2. **任务单数字不亲验**：「四件」顺手就想照抄进报告，mtime 盘点才发现是 6 件。为什么差点漏：登记任务下意识信任上游实测数字。对策已落报告（实测 6 件并更违）。

## 顿悟

- **裁剪扫描面 = 双边风险**：裁多了静默漏登记（#605→#619），放多了登记洪水（863KB 巨件）。白名单+内部目录豁免是唯一的稳态结构——落点显式枚举，内部产物按命名约定（`_` 前缀）自动豁免。
- **state 播种是灰度开关**：给扫描器扩容时，「把存量标为已见」等价于 feature flag 的灰度——新旧切开，只放新流量过闸，验证口径才干净。

## 过程资产

- wiki 仓 commit `d93853fcf`：`kdo-tools/watch_inbox.py`（SCAN_SUBDIRS/SKIP_SUBDIR_PARTS 白名单化）、`kdo-tools/tests/test_watch_inbox.py`（+test_scan_whitelist_subdirs）、`90_control/notification-coverage-matrix.md`（行 9 同步，§3.19）
- `.kdo/inbox_state.json`：12 键播种（gitignore 内，本机状态）
- 任务单 `60_feedback/tasks/task_20260902_huangyaoshi-watch-inbox-pipeline-dirs-fix.md` 五字段执行报告
- 验证现场：实跑输出「待编排看板更新: +6」、production-queue.md L583-588 六行、重跑 exit 0 零输出、pytest 4 passed

## 元反思

下次怎么做才能不一样：凡「改扫描面/监控面」任务，开工第一步固定跑「存量影响盘点」（当前在扫什么+改后会多扫什么+state 覆盖差集），把首跑副作用算清楚再动手——本次是事后补救式盘点，下次前置。「防重意图」要跟着机制走：EXCLUDE_DIRS 删除时先问它当年防的是什么，把答案下沉到新结构里，而不是一删了之。

## Truman复盘

### 逐轮映射

| 轮次 | 人做什么 | 双三角要素 | AI做什么 | 双三角要素 |
|:--|:--|:--|:--|:--|
| 老朱问「偶遇→inbox→拉起工作流正常吗」 | 提问触发全链体检 | 方向 | （王语嫣）逐环实测定位第二环断裂 | 判断 |
| 王语嫣立 #619（证据+白名单口径+继续排除大目录） | 定边界定验收 | 方向+校准 | 施工+盘点存量+播种基线+实跑验证 | 场景+执行 |
| E040 门禁（一次过） | 机制代人校准 | 校准 | 交付物先 commit 再 complete | 执行 |

### 飞轮效应

加速了「老朱一句话提问→逐环实测→当天修复→当晚登记恢复」回路：从提问到 6 件补登记落看板同日完成。副产品：扫描面有了回归测试，下次裁剪有门禁；矩阵行 9 第三次同步，§3.19 纪律执行成本继续下降。

### 对照实验

- 无人（纯 AI）：白名单一加直接跑，41 件洪水登记（含 knowledge/ 重复派发），王语嫣看板被陈旧件淹没——播种决策来自「#605 巨件事故」的人为历史记忆。
- 无 AI（纯人）：state 差集盘点+mtime 切分+三路验证，手工对账以小时计。
- 合在一起：人给边界（哪些目录是落点、哪些继续排除），AI 做差集盘点和基线对齐——监控面变更的完整形态。

### 下次改进

- Agent 自身：监控面/扫描面变更任务，「存量影响盘点」列为第 0 步写进肌肉记忆（本次事后才做）。
- 方法论卡候选：「扫描面变更双边风险」——裁剪有漏登记风险、放开有洪水风险，白名单+命名约定豁免+存量播种三件套（待王语嫣裁定是否入库）。
