# 建议书：minimax 额度耗尽，多模态 VLM 管线单点失效

- **现象**：老朱 09-05 直派单指定"用 minimax 额度 OCR+VLM"，实测 `api.minimaxi.com` 返回 `status_code 1008 insufficient balance`（2026-09-06 12:00 原始响应，洪七公 AI组织行为学口述 OCR 单）——额度非"紧张"而是已耗尽，仓内 `describe-images-minimax.py`/`cap_hub.vlm`/`long-image-ocr`/`run_vlm_extract.py` 全部走该 key，一次失效=视觉管线全断。
- **在哪发现**：洪七公 OCR 直派单执行中；`40_outputs/capabilities/skills/long-image-ocr/batch_ocr.py` 与 `00_inbox/人机协作双三角/run_vlm_extract.py` 同 key 路径，均会复现。
- **建议方向**（可选）：①账户充值或换 key（决策在老朱）；②短期兜底=实例自身视觉读渲染页图（本次实测 2087/2087 零差异，可作正式降级路径写入 `long-image-ocr` skill）；③`run_vlm_extract.py` 等脚本里 `wiki/.env` 读 key 的路径已失效（.env 不存在，key 实际在环境变量），顺手修可防下次误判"key 丢失"。
