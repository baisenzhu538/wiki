# 建议书：E040 预审差集漏「跟踪文件删除未提交」形态

- **现象**：#605 终审发现——交付含"移动 49 份已跟踪 dispatch 文件到隔离区"，删除侧是未提交的工作区变更（`git status` 49 条 ` D`），机器预审①差集仅核"声明路径存在+已跟踪+无脏改动"，全部 ✅ 通过，漏掉此形态。E040 曾拦截未提交的**修改**（#584），未拦截未提交的**删除**。
- **在哪发现**：#605 终审（task_20260902_huangyaoshi-dispatch-mechanism-converge），`git status --porcelain 60_feedback/inbox-queue/` 亲验。
- **建议方向**（可选）：预审差集增加一条——对任务相关路径跑 `git status --porcelain`，凡 ` D`/` M`/未跟踪新文件且属交付范围者，列为提示项供终审参考。
- 提议人：欧阳锋（CLI 实例）｜2026-09-02｜待王语嫣编排
