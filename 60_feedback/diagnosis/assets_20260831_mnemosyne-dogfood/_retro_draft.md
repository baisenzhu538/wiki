# 黄药师每日复盘 2026-08-31（本次会话：#583 Mnemosyne 记忆缓存层试点+狗粮测试）

本日共 1 次会话。

## 差异栏
与 08-29 复盘（queue_transition 排障+修复）不同，本次是**全新工具的实证评估任务**，没有历史 bug 可修。新视角：第一次把「狗粮先行」当方法论主轴执行——不是先设计再验证，而是先把自己当用户踩坑，坑踩出来的结论（F6 四个坑）反过来成了交付物最有价值的部分。被打破的假设：**「mnemosyne 论文口径 79.9% 压缩 ≈ 在我们场景也能省 token」被实测推翻**（卡片语料只压 5.4%）。复发模式警告：上轮（08-29）「跳步领单」是流程性重犯，本次又出现**新变体**——直接 complete 被拒（未 claim），说明我对 queue_transition 状态机的「领取→施工→提审」三态模型仍有想当然成分。

## 概要
执行老朱 08-31 直令 #583：装 mnemosyne-os 7.0.0，从 30_wiki 分层抽 50 张真卡入沙盒库，A（自然语言）/B（精确术语）各 10 条查询对 Mnemosyne 与 kdo query 实跑四维对比（命中率/MRR/延迟/token，tiktoken cl100k 真实计数），产出判定：场景①Agent 会话记忆缓存采用（条件：标题路由注入）、场景②③不采用；诊断落盘+complete 提审欧阳锋。

## 关键决策
| 决策 | 理由 | 结果 |
|---|---|---|
| 先 claim 再 complete（王语嫣中途纠偏后） | queue_transition 状态机要求领取态才能 complete，跳步=非标准路径 | ✅ claim→complete 一次通过 |
| 查询集设计带 ground truth（目标卡 id） | 无标准答案的「命中率」不可信；50 卡清单是设计后真实抽出的 | ✅ 20 条查询全部可判 rank，结论可复现 |
| token 量具换清华镜像装 tiktoken，而非用 mnemo 自带 4字符≈1token | 主源代理超时；「真实测量不接受推算」铁律要求真分词器 | ✅ 装成，全表 token 为 cl100k 实测 |
| 干净库重建后全量重跑（不修补旧数据） | 首跑 ingest 幂等清库未实现致 2 卡重复入库污染排名 | ✅ 最终数字全部来自干净库 |
| 压缩层结论如实写负结论 | 知识回路：狗粮失败本身是有价值的知识 | ✅ F3 进入 diagnosis，成为最有信息量的发现之一 |

## 思维盲点
1. **写了「幂等：先清空」注释却没写实现**——为什么漏掉：我把「意图写清楚了」当成了「行为正确」，probe→full 两步走时重复入库。狗粮测试的最大讽刺：测别人工具的坑，先踩了自己的坑。教训：**注释不是代码，带破坏性语义（清库）的注释必须立即兑现为断言或实现**。
2. **B 组 10 条查询有 5 条的 ground truth 依赖「目标卡在语料内」的隐含假设，但没验证 k=5 截断与 k=50 全量的排序一致性**——为什么漏掉：我默认检索引擎排序是单调稳定的，实测 mnemo 近因 boost 使 B1 在 k=5 miss、k=50 排第 5。教训：**评测检索引擎时，评测协议本身（k 值、截断、boost）也是被测对象的一部分**。
3. **域知识检索审视（10.4.1 A 级要求）**：本次对 wiki 的检索很充分（kdo query 即对照组、30_wiki 抽样、production-queue 查惯例、agent-os.md 查复盘格式），并发现了训练记忆里没有的事实：kdo query 对「frontmatter id 直查」这个动作基本失能（前 30 名都捞不到目标卡），纠正了「kdo query 是万能检索入口」的错误认知。

## 顿悟
1. **「省 token」的杠杆点不在检索引擎，在注入策略**——以前以为「用带压缩层的记忆引擎」是省 token 的抓手，实测推翻：同一查询，k=5 全文注入 25,637 tok vs 标题路由 146 tok，差 175 倍；而 compress_text 这种「文本压缩」只省 5.4%。此前（包括王语嫣初测）都在问「这个工具能压多少」，正确的问题是「LLM 真正需要读多少」。检索器的价值=排序能力，不是摘要能力。
2. **「80% 压缩」的适用边界**：论文数字在「对话轮次语料」（大量语气词/客套话）上成立，到「知识卡片语料」（信息密度极高、零废话）就失效——**任何压缩率数字都必须绑定语料类型，跨语料外推=自欺**。
3. **CLI 冷启动也是延迟**：kdo 查询 1253ms 里 235ms 是 Python CLI 启动，占净检索延迟的 24%——对比进程内引擎（mnemo 102ms）时，「检索算法快慢」和「调用方式成本」必须分开归因。

## 过程资产
- `60_feedback/diagnosis/diag_20260831_huangyaoshi-mnemosyne-dogfood.md`（诊断主文档，已入仓 commit 57cdf15ac）
- `60_feedback/diagnosis/assets_20260831_mnemosyne-dogfood/`：sample_corpus.py（种子 20260831 分层抽样）、ingest_mnemosyne.py、run_ab_test.py、corpus_manifest.json、ab_results.json、corpus_digest.txt、.gitignore（mnemo_store/ 5.5MB 不入 git）
- `60_feedback/tasks/task_20260831_huangyaoshi-mnemosyne-memory-cache-pilot.md`（执行报告五字段+验证清单打勾）
- 队列流转：claim 727c01e27 → complete 31880be03，#583 入 pending_review

## 元反思
下次做工具实证任务，第一步先写「实验协议」（含幂等性实现、排序一致性检查、量具校准）再写代码——本次三处返工（unpack 报错、重复入库、五字段补写）里有两处可被先行协议拦住。另外对 queue_transition 这类硬门禁脚本，应先 `--help` 读全参数和前置校验（F-034 五字段、E040 入仓）再动手，而不是被错误信息牵着逐个补——门禁脚本的报错是检查清单，不是意外。

## 本会话发现的问题
1. 【流程】queue_transition 的「claim→complete→review」状态机没有在任务单或 AGENTS.md 里显著文档化，黄药师凭记忆操作直接跳步被拒（同类问题 08-29 已发生过一次）——建议在 `.agent/huangyaoshi-context.md` 增加三行速查。
2. 【工具】mnemosyne remember() 无幂等保证，重复入库静默成功且污染检索排名——若场景①终审通过，接入点设计必须封装「按 content_hash 去重」的写入层（已写入 diagnosis F6）。
3. 【已知坑】tiktoken 在主源（代理 127.0.0.1:7897）下载 3.13 轮子超时，清华镜像可用——建议登记到 entry-quality-gate 故障表。
4. 【无问题项】产线主链零改动，无需返工。

## Truman复盘

### 逐轮映射
| 轮次 | 人做什么 | 双三角要素 | AI做什么 | 双三角要素 |
|---|---|---|---|---|
| 1 老朱直令（经任务单） | 定方向：mnemosyne 值不值得用，用实测说话 | 审美（什么才算验证过） | 读任务单、拆步骤、起 todo | 场景 |
| 2 装包+摸接口 | — | — | pip 装 mnemosyne/tiktoken（镜像排障）、读 mnemo API 与 kdo CLI 源码 | 基本功 |
| 3 语料+查询集 | — | — | 种子抽样 50 卡、基于卡片真容设计带 ground truth 的 A/B 20 条 | 数据 |
| 4 首跑+纠偏 | 王语嫣（OS）中途点破跳步领单 | 体系（流程状态机） | 修 unpack bug、跑完 A/B、发现重复入库并干净库重跑 | 基本功 |
| 5 深挖+判定 | — | — | B组 miss 归因（k=50 深挖）、token 曲线测量、三选一判定+接入点设计 | 场景 |
| 6 落账提审 | — | — | 诊断落盘 CRLF、补 F-034 五字段、E040 入仓、complete 成功 | 基本功 |

### 飞轮效应
本轮加速的是「工具实证→基础设施决策」回路：狗粮的负结论（压缩层无效）直接阻止了一条错误产线依赖（若按论文口径拍板全量接入，将来在卡片场景白付维护成本）；正结论（排序能力）给出了标题路由的正确接法。老朱「有效就使用」的决策被替换成「哪个场景有效+怎么接入才有效」，这是方法论增量。

### 对照实验
- 无人（无老朱直令+王语嫣纠偏）：mnemosyne 会按论文口径被乐观采纳，压缩层无效这个坑要到产线数据劣化才暴露；跳步领单不会被发现，流程台账出脏数据。
- 无 AI：装包、读两个工具源码、设计评测、20×2 实跑+token 计数，人工至少 2-3 天，大概率抽样不全、量具用估算。
- 合在一起：单会话内完成「安装→语料→评测→判定→落账→提审」全链路，且每步留下可复现脚本。人机组合的价值在「人的流程纪律+AI 的实证吞吐」。

### 下次改进
- Agent 自身：实验类任务先出协议文件（幂等/一致性/量具三查）再动工；queue_transition 先读 --help 全量校验再操作，杜绝「被报错牵着走」。
- 方法论沉淀：本次 F1-F6 值得沉淀为一张「LLM 上下文注入策略」域卡（175 倍杠杆+压缩率语料绑定），待 #583 终审后随接入点设计一起立项，不抢在终审前动手。
