# 技能进化日志

> 最上方追加当天的 Keep / Improve / Add / Stop。

---

## 2026-08-20

**Keep（继续做）**：
- 行为改变类修复标配前后对比实测——#382 的对比实测不止满足验收，还真抓到"误杀素材层"连环缺陷（只补 import os 会把无 frontmatter 的 src_*/README 全部剔出检索）
- 检测规则由真实失败样本驱动：乱码正则拿真实 mojibake 样本测过才定区间（`[U+0080–U+00FF]{2,}` 而非凭直觉的 `[À-ÿ]`）
- 争议方案不擅动：A/B 悬而未决时不动 promote 逻辑、报裁决——王语嫣改判 A 后证明等待是对的
- 杀进程前先验身份（CommandLine 匹配）再动手，15 个 MCP server 逐一确认

**Improve（改进）**：
- 代码类交付的"完成"定义含 commit——提审前双仓 git status 自查（本次吃欧阳锋 FAIL：修复未提交=不存在，门禁是兜底不是第一道）
- 不可见字符（U+FFFD 等）一律转义写法 + 写后字节级读回验证——自己的输出通道会吞字面量，发生两次
- 门禁类代码的 `except: pass` 至少加计数/日志——_filter_by_trust 的 NameError 被静默吞掉，过滤器从未生效却看起来正常
- 门禁有效性=声明完整性：code_files 必须用门禁可检的路径形态（绝对路径），"KDO仓:" 前缀会 fail-open 假通过

**Add（新技能/工具）**：
- `_content_issues()` 内容校验三拦截（标题乱码/空壳正文/LLM 占位）——可复用到任何自动产卡管线
- `check-draft-aging.py` 存量 draft 超龄巡检（advisory 恒 exit 0 的报警形态：清单是本体，不阻断）
- `_label_unreviewed()` 检索结果未审标注（只标注不降权，CLI+MCP 双端）
- pending-cards 待编排区模式：复用 watch_inbox 既有扫描做通知，零新增通知代码

**Stop（停止做）**：
- 不要把"代码改完+测试过"当交付终点——未 commit 的修复不存在
- 不要在 fail-open 的门禁格式上叠新格式——每种声明格式都要先验证门禁真的能拦

---

## 2026-06-14

**Keep（继续做）**：
- 每个域完成加一张"框架→KDO 工具链"映射卡——建模域和业务公式域都做了，效果显著
- 从源材料"旁白"提取金矿——不是转录框架，是找操作心法、失败案例、判断口诀
- Agent 交叉审计——王语嫣审老顽童、老顽童审王语嫣，比欧阳锋单点审查快

**Improve（改进）**：
- API 异常时先查公告再调参——K2.7 折腾 3 小时是反面教材
- 改 provider 前三步确认（查 npm→定 URL→改 config）
- 复盘必须更新所有 6 个文件，不能只写每日复盘

**Add（新技能/工具）**：
- Ingestion pipeline（P0/P1/P2 分级 + 王语嫣门禁 + inbox 监听器）
- 五道自动门禁（confidence/Synthesis/出链/跨域/入库）
- 免费 web_search 工具（cn.bing.com 直连）
- 图谱 hub 降权（入度 >30 节点边权重 ×0.3）

**Stop（停止做）**：
- 用户说"不要乱改"后还在调参——强制冻结机制
- 改 model 名作为诊断手段——该查日志和公告

---

## 2026-06-09

**Keep（继续做）**：
- 从个案提炼工厂标准——外援的广冷整理→交叉验证检查单+case卡+项目标准升级
- 读完完整素材再判断——半肥猫八步的深度来自读完整口述稿
- 欧阳锋讨论稿的"对齐判断"比"直接动手"更值钱——三份讨论稿对齐后才动手

**Improve（改进）**：
- 审计报告的"反馈摩擦"没说错——我有 `kdo feedback` 但缺少低门槛入口
- 自动标签脚本跳过了老顽童"选值"环节——改为建议模式是对的

**Add（新技能/工具）**：
- `kdo role <agent>`：角色编译——从三个分散文件编译为一段 system prompt
- `kdo trace <id>`：编译溯源——原料清单+版本+过期检测
- 弱结构素材提取骨架（四问法）：解决马易水水类素材无天然框架的问题
- `append_feedback_template()`：文章自动附反馈模板

**Stop（停止）**：
- 不要在审查和开发之间频繁切换——现在做到了"先审查定结论，再写代码"
- 不要把自动标签直接写入卡片——改为 `kdo tag suggest --write` 建议模式


---

## 2026-06-07

**Keep（继续做）**：
- 审查前先跑自动门禁（kdo lint + v1.5 validate），让数据说话，再给判断
- 写代码时同步写测试，确保回归安全（全天 455 pass, 0 new failures）
- 先读完整源文件再做判断（纪浩五层体系是通过读完整口述稿才看清的，只看片段会误判）

**Improve（改进）**：
- 审查和 infra 开发在同一会话交叉进行，导致注意力碎片化。下次先完整审查→写审查报告→再做 infra
- kdo quick 命令写完就测试通过，但没有在会话早期就想到它。这个命令的灵感来自审查老顽童纪浩批次的痛点——应该在感受到痛点的当下就记下来，不在 session 末尾才补

**Add（新技能/工具）**：
- `kdo encapsulate` 渐进式披露编译：manifest.yaml 支持 L1-L4 四层知识注入
- `kdo lint --accept-baseline`：把历史债收入基线，只看新增问题
- `kdo skill validate`：eval cases 结构校验 + 覆盖率报告，publish 前质量门
- `difflib.SequenceMatcher`：跨卡文本相似度检测，用于发现 copy-paste 模式

**Stop（停止）**：
- 不要再绕过 `kdo produce` 管线直接创建 artifact 文件（I-3 规则防呆）
- 不要再跳过 OCR 检查——source_refs 中的图片必须有 `_paddle_ocr.txt`（I-6 规则防呆）
