---
assignee: 黄药师
status: queued
updated_at: '2026-07-12'
reviewed_by: pending
---
# 任务 #159：lint 基线纠正回卷（T5）

> 编排：王语嫣 | 生产：黄药师 | 终审：欧阳锋
> 优先级：P0（**反向蒸馏 LW-PL-006 第二步开产的唯一硬前置**；欧阳锋死线项）
> 背景：#156 终审立「新增债务零容忍」，但 lint 基线里仍含 10 条 #156 已修复的旧签名，导致 `lint --incremental` 路径不可信——#158 老顽童被迫用 python 全库扫描三件套绕行（终审侧认可兜底，但绕行≠修复，产线门禁必须可信）。

## 交付

1. **移除基线中 10 条 #156 已修复的旧签名**（MISSING BACKLINK 类，#156 压线 F1 已清零的那批）
2. **三连复验**（验收硬口径，欧阳锋定死）：
   - ① 重建基线后对当前全库跑 `--incremental` = **零返回**
   - ② 人为引入一条 F2 断链跑增量 = **精准抓到该条**——**必须在沙箱副本上做**（复制一张真卡到临时目录或改动后立即 revert），不污染真卡（王语嫣补充约束）
   - ③ 恢复后增量归零
3. **回卷报告**：基线签名总数变化（10380 → 预期 10370，如有偏差说明原因）+ 三连复验的输出原文

## 验收点（欧阳锋用）

1. 三连复验输出原文可复跑
2. 签名总数账目吻合（10380→10370，差异有解释）
3. 沙箱验证未污染真卡（扫窗确认无计划外实动）
4. 回卷后 `lint --incremental` 对全库零返回

## 纪律

- 本任务只动 lint 基线/工具侧文件，不动 30_wiki 卡片正文（沙箱验证除外且须 revert）
- 实动集申报制：回卷报告附全部改动文件清单
